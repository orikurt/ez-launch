<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sadna.ez_launch"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
	<uses-permission android:name="android.permission.CALL_PHONE"/>

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- START OF WIDGET RECIEVER 
        <receiver
            android:name="EZLaunch"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/watch_widget_provider" />
        </receiver>-->
     <receiver android:name=".widgets.contacts.sizes.ShortcutWidget_4_4" android:label="@string/widget4x4">
			<intent-filter>
				<action   
					android:name="android.appwidget.action.APPWIDGET_UPDATE" />
			</intent-filter>
			<meta-data android:name="android.appwidget.provider"
				android:resource="@xml/ez_launch_widget_provider4x4" />
			<meta-data android:name="LauncherMetadata.Requirements.APIVersion"
				android:value="2" />
			<meta-data android:name="LauncherMetadata.Requirements.Scrollable"
				android:value="true" />
		</receiver>		
		
		<activity android:name=".ConfigurationActivity">
			<intent-filter>
				<action android:name="sadna.ez_launch.PREFS" />
			</intent-filter>
		</activity>
		
		<service android:name="ContactWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- END OF WIDGET RECIEVER -->

       
       <!-- Mor your old Service is here 
        <service
            android:name=".StatisticsService"
            android:enabled="true" />
            -->
    </application>

</manifest>